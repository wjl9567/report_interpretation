server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # /api 由 Ingress 或外部反向代理转发到后端，容器内不配置 proxy_pass
    # 若需容器内代理，可在 docker-compose 或 K8s 中挂载覆盖此配置
}
