apiVersion: v1
kind: Service
metadata:
  name: report-ai-backend
  namespace: report-ai
  labels:
    app: report-ai
    component: backend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: report-ai
    component: backend
